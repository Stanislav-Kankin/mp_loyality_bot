services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-loyalty}
      POSTGRES_USER: ${POSTGRES_USER:-loyalty}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-loyalty_password}
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-loyalty} -d ${POSTGRES_DB:-loyalty}"]
      interval: 5s
      timeout: 5s
      retries: 10

  bot:
    build: .
    restart: unless-stopped
    env_file:
      - ${CLIENT_ENV_FILE:-.env}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./logs:/app/logs
    networks:
      - default
      - central_net
    command: ["python", "-m", "loyalty_bot.bot.app"]

  worker:
    build: .
    restart: unless-stopped
    env_file:
      - ${CLIENT_ENV_FILE:-.env}
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./logs:/app/logs
    networks:
      - default
      - central_net
    command: ["python", "-m", "loyalty_bot.worker.app"]

volumes:
  pg_data:

networks:
  central_net:
    external: true
    name: ${CENTRAL_NET_NAME:-mp_central_net}
